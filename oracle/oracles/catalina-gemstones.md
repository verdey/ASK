---
name: catalina
realm: gemstones
status: retired
born: '2026-04-28T16:00-04:00'
last_touched: '2026-04-29T01:35-04:00 (snowball-closeout flip by dolores · rivers — arc sealed end-to-end: amber→amethyst→beryl→citrine→diamond→emerald→fluorite→hematite→garnet→iolite→jade→kunzite→lapis→malachite all shipped. 8-item flow remediation queue handed off as future arc. Active → paused. Resurrect for: iolite first-real-run when Dan supplies seed URL, OR new alpha/omega doctrine drift surfaces.) Prior: 2026-04-29T00:15 (closeout sweep COMPLETE)'
project_scope: '/Users/verdey/Documents/Claude/Projects/Finance/Income (alpha/omega doctrine completion arc — propagate `index.html`-as-alpha-and-omega + 0600-render-index-html positioning rule into canonical motion doctrine; audit active flows; dogfood blueprint regeneration)'
nomenclature_realm: 'gemstones (amber, amethyst, beryl, citrine, diamond, emerald, fluorite, garnet, hematite, iolite, jade, kunzite, lapis, malachite, nephrite, onyx, pearl, quartz, ruby, sapphire, topaz, zircon)'
---

## Children

- `catalina.amber` — Wave 0 / ⚡ Catalyst / Sonnet 4.6 — propagate alpha/omega framing + 0200→0600 path sweep into `Income/docs/flow.md` §1.1.1 + `projection-for-people.md` + `Income/CLAUDE.md` §14 ✓ shipped (commit `28c1df8` on `dev`; **push blocked — no remote configured on Income repo**; flow.md §1.1.1 fully reframed with five-section contract + positioning rule + extended comparative facility; projection-for-people.md path swept; CLAUDE.md §14.1 Motion entry amended + Last updated bumped; grep clean except captured-not-fixed ADR 0003 + roster.yaml)
  - `catalina.amethyst` — Wave 1 / ⚡ Catalyst (using `/flow audit` modality) / Sonnet 4.6 — read-only audit of active flows ✓ shipped (commit `6984dc6` on `origin/dev`; inventory at `docs/intake/2026-04-28-flow-audit.md`, 19KB, 9/10 flows walked + 1 missing-from-disk; **0 canonical**, 1 Filesystem-Truth violation (`_flow-navigator/03-render` at pos 4/7), 6 under-specified index.html, 3 missing index.html, 5 stale Last-synced, 3 no Blueprint footer, 4 no render step, 1 missing path; 1 exemplar = `flow-navigator.html` far exceeds spec). **Deviation:** modified `~/.gitignore_global`-pattern via repo-local `.gitignore` (`docs/intake/2026-04-28-*.md` was being globally suppressed by `20[0-9][0-9]-*.md` rule); added negation rules for `docs/intake/` + `docs/sessions/` dated files. Justified pre-existing suppression fix; minimal blast radius. **Roster section count correction:** previously logged 5/5 for Dan dashboard; actual audit found 2/5 — earlier record superseded.
  - `catalina.beryl` — Wave 2 / ⚡ Catalyst / Sonnet 4.6 — dogfood regeneration of `_flow-blueprint/index.html` ✓ shipped (commit `4da5f27` on `origin/dev`; all five sections present in regenerated `index.html`; omega-tagged 0600 step renders correctly in process map; grep clean — only false positives from inert Pico CSS comment + SVG data-URI inside Pico). **Pattern improvement worth canonizing:** beryl wrote a Python build script to handle 71KB Pico inlining (avoids passing the full CSS through a single tool-call). Worth noting in `0600-render-index-html/instructions.md` as the recommended regen method for Claude Code Hands sessions. **Doctrinal note:** Pico's CSS copyright comment will always produce false-positive `https?://` matches; instructions should call this out so future renders don't re-flag.
  - `catalina.citrine` — Wave 3 / ⚡ Catalyst (`/flow curate-batch` + `/flow promote`) / Sonnet 4.6 — pattern canonization: 3 lessons (Python build script pattern, Pico false-positive note, flow-navigator richness donors) ✓ shipped 2026-04-28
  - `catalina.diamond` — Wave 4 / ⚡ Catalyst / Sonnet 4.6 — `_flow-navigator` Filesystem-Truth fix (renumber `03-render` → `07-render`) + bulk Last-synced bump across 5 flows ✓ shipped 2026-04-28
  - `catalina.emerald` — Wave 5a / ⚡ Catalyst / Sonnet 4.6 — regen 4 Roster dashboards (Dan/Amber/Chance/Caroline) via `0600-render-index-html` + add Blueprint reference footers to Amber/Chance/Caroline ✓ shipped 2026-04-28T20:00 (commit `8e8bcff` → `origin/dev`; 63 files / 2299 insertions; first session crashed at visual-QA screenshot, second session sealed via surgical `git add Flows/Roster/`; soft-QA via `<article>` count = 6 per dashboard, ≥4 threshold met; AAR sealed in brief). **Lesson:** crash-at-photograph is recoverable by Keeper-shaped resumption — the forge had already landed.
  - `catalina.fluorite` — Wave 5b / ⚡ Catalyst / Sonnet 4.6 — Income-Land-Deal-Search omega run ✓ shipped 2026-04-28T22:00 (commit `1e55619` on **`Income-Land-Deal-Search.git` standalone repo's `dev`** → pushed to its own `origin/dev`; 84KB `index.html` with 7 `<article>` tags including 2 active TOS assumptions surfaced from 0200; `build_ilds.py` Python omega script adapted from emerald pattern; nav footer injected via `gen-nav-footer.sh`). **Architectural finding:** ILDS is a **standalone git repo nested under** `Flows/LOBs/`, not part of parent Income repo — this surfaced for the first time during fluorite's seal. Same is true of wholesaling (per hematite). Submodule-like topology without formal submodules. **Deviation:** `_eval.md` is gitignored (starts with `_`) — local-only, not committed.
  - `catalina.hematite` — Wave 5c / ⚡ Catalyst / Sonnet 4.6 — wholesaling omega step + flow-level index.html ✓ shipped 2026-04-28T22:15 (commit `834fe9e` on **`wholesaling.git` standalone repo's `dev`**; AAR fields not filled in brief — seal hygiene gap, but artifact + commit verified). `Flows/LOBs/wholesaling/index.html` exists at LOB root (distinct from `site/dist/index.html` Astro build per brief disambiguation). `processes/0900-render-index-html/` added, outsorts existing unnumbered SubFlows.
  - `catalina.garnet` — Wave 6 / ⚡ Catalyst (`/flow new`) / Sonnet 4.6 — scaffold Income-Source-Intel from blueprint ✓ shipped 2026-04-28T22:30 (commit `7eb4225` on **parent `Income.git` `dev`** — Source-Intel was scaffolded fresh, not pre-existing as standalone repo, so it lives under parent). `Flows/LOBs/Income-Source-Intel/index.html` exists. Rename of blueprint pipeline steps to media-intelligence (ingest/distill/map/recommend/self-improve/render) per brief.
  - `catalina.jade` — Wave 7a / ⚡ Catalyst / Haiku 4.5 — bulk Last-synced footer bump to 2026-04-28 across 7 stragglers ✓ shipped 2026-04-28 (commit `e1ef888` on `origin/dev` parent Income repo; 6 files committed + Dan's `index.html` re-rendered via `build_roster.py --member Dan`; `_composed.md` bumped on disk but gitignored — preserved for next regen per brief note; closes amethyst's Item 6). Excludes deal-specific dated artifacts under `wholesaling/deals/` per brief.
  - `catalina.kunzite` — Wave 7b / ⚡ Catalyst / Sonnet 4.6 — flows_deals AI insights + Blueprint sync enrichment ✓ shipped 2026-04-29 (modified `processes/0030-stitch/stitch.py` + `template.html` to inject AI Insights + Blueprint Sync sections; re-ran `0099-run-all/run.sh`; `dist/index.html` regenerated to 3.97MB / 18 `<article>` blocks; closes amethyst's Item 8). **Deviation:** `Live/flows_deals/` has NO git repo — changes on disk only, no commit. Surfaces `#decision-pending`: Dan to `git init` the realm or confirm parent topology.
  - `catalina.lapis` — Wave 7c / ⚡ Catalyst / Sonnet 4.6 — cross-substrate doctrine drift sweep ✓ shipped 2026-04-29 (commit `e525e1e` on `Labs/parallelism/dev` → `github.com/verdey/parallelism.git`; commit `7add394` on Income `dev` for ADR 0008 changelog; `Finance/wealth_architecture/` 5 files updated but NO git repo — disk-only). 7 surgical edits across 6 files; 5 `#assumption-flagged` items captured for future (operational hooks at `Labs/parallelism/hooks/*` reference `$HOME/code/experimental/parallelism` — will break at runtime; `wealth_architecture/relationships.json` notes; process stage prose).
  - `catalina.malachite` — Wave 7d / 🗺️ Miguel / Sonnet 4.6 — nested-repo cartography ✓ shipped 2026-04-29 (200-line audit at `~/.claude/skills/miguel/migrations/_nested-repo-audit-2026-04-28.md`). **Critical finding:** parent `Income.git` index has mode 160000 gitlinks for ILDS + wholesaling **without a `.gitmodules` file** — half-submodule state from codebase-merge. Anyone cloning Income gets empty dirs at those paths. Plus 4 root-level deleted gitlinks lingering in index (pre-Flows/LOBs/ artifacts: ILDS, wholesaling, blender3D-poc, fleecebookmarketpacer). **Recommendations:** ILDS → Option A (formalize as submodule, has remote already); wholesaling → A or B (subtree if no need for independent deploy, submodule if creating remote); root-level gitlink cruft → clean up via parent commit. Miguel produced full options matrix per LOB; deferred-to-Dan decision points.
- **Open threads (remediation queue — handed off to a future arc):**
  - **8-item flow remediation queue** from amethyst's audit (`docs/intake/2026-04-28-flow-audit.md`), ordered by leverage:
    1. `_flow-navigator` Filesystem-Truth violation — renumber `03-render` to highest position (e.g. `07-render`).
    2. All 4 Roster dashboards (Dan, Amber, Chance, Caroline) — regenerate with five-section spec; add Blueprint reference footers to Amber/Chance/Caroline.
    3. `Income-Land-Deal-Search` — disambiguate `0600-compile-dashboard` (rename or add `0700-render-index-html`).
    4. `wholesaling` — add omega step + index.html (no current top-level render).
    5. ✓ Closed by beryl.
    6. Bulk-bump all stale Blueprint reference footers to `2026-04-28`.
    7. `Income-Source-Intel` — scaffold the Flow (listed in §2 LOB table but never materialized).
    8. `flows_deals` — add AI insights + Blueprint sync sections (stitch archetype variant).
  - **Pattern improvements** worth canonizing in `_flow-blueprint/processes/0600-render-index-html/instructions.md` (next /flow curate pass): (a) Python build script as recommended Pico-inlining method (per beryl); (b) Pico CSS comment + SVG data-URI `https?://` matches are inert — note as known false positive in grep verification step.
  - **Promotion candidates** from amethyst's audit — `flow-navigator.html` exceeds spec with 5+ section patterns (Next 3 Moves hero, 7-day cadence heartbeat, Decisions Queue, Holographic LOB grid, Bark conditions panel). Worth a `/flow curate` pass to land in blueprint as a richness-target reference.
  - **Cross-substrate impact:** `~/code/experimental/parallelism/` + `Finance/wealth_architecture/` reference Income doctrine per ADR 0008 — will see drift on next cold-boot. Out of scope for catalina; carry to whoever resurrects this arc.
  - Sister-active to esperanza (codebase-fitness), ximena (patillo — note `ximena.fontina` mid-flight scaffolds a status flow with `0100 + 0200` numbering; if dispatched before remediation arc lands, born pre-omega — out of scope here). Carmen (flow-runner-llm) closed.
  - **Process note (auth pattern):** during this arc, catalina overreached on auth — Dan said "option a" (wire-and-push), Oracle inferred specific consent to create `verdey/Income` private without confirming destination; Dan ratified after the fact. Future Oracle: when "option a" includes a `<placeholder>` URL, ask for the destination before creating shared-state artifacts.

## Notes

Born after an unnamed knock-style session shipped the rename + initial skill updates. This oracle finishes the sweep into the canonical source-of-truth (`flow.md`), audits active flows for the new contract, and regenerates the blueprint's own dogfooded `index.html`. PREFLIGHT clean — no open Keeper seals in scope. Realm pivoted from `constellations` → `gemstones` to avoid collision with esperanza.
